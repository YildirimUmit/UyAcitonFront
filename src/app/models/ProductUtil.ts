import {Size} from "./Size.enum";

export class ProductUtil {
    productUtilId: number;
    productId: number;
    price: number;
    color?: string;
    size?: Size;
    quantity: number;
    mage: string
}
